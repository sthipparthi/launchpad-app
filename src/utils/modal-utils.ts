import { Dispatch, SetStateAction } from "react";
import { openModal } from '../actions/launch-pad-actions';

export const handleOpenModal = (setModelActionType: Dispatch<SetStateAction<String>>) => {
    setModelActionType
}